const Employee = require('./employee');

class Intern extends Employee {
    constructor (school) {
        super (Jake, 80, 'jake@email.com');
        this.school = school;

    }
}

getSchool();
getRole();
